import { SafeAreaView, Text } from "react-native";

const SignUpScreen = () => {
  return (
    <SafeAreaView
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text
        style={{
          color: "white",
        }}
      >
        123sadffs
      </Text>
    </SafeAreaView>
  );
};

export default SignUpScreen;
